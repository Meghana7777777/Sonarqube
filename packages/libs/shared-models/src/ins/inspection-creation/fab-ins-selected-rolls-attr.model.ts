export class InsFabInsSelectedRollModelAttrs {
	sWidth: number; // supplier width
	mWidth: number; // measured width
	sLength: number; // supplier length
	constructor(
		sWidth: number,
		mWidth: number,
		sLength: number,
	) {
		this.sWidth= sWidth
		this.mWidth= mWidth
		this.sLength= sLength
	}
}