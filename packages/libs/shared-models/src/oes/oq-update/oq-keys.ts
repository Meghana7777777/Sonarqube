// This variable is client specific. For norlanka we have the least saggregation is at Mo-line + color level only
export enum OqKeys {
    MO_LINE = 'MO_LINE',
    COLOR = 'COLOR',
    SO_LINE = 'SO_LINE',
}