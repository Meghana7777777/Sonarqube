import { SwaggerConfig } from './swagger.interface';

/**
 * Configuration for the swagger UI (found at /api).
 * Change this to suit your app!
 */
export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'Xapperal-SPS',
  description: 'Services for sewing-planning',
  version: '1.0',
  tags: ['KMS'],
};
